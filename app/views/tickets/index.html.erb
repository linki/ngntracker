<% title "Tickets" %>

<table>
  <tr>
    <th>ID</th>    
    <th>New</th>
    <th>Name</th>
    <th>Category</th>
    <th>Creator</th>
    <th>Assigned to</th>
    <th>Updated at</th>
  </tr>
  <% for ticket in @tickets %>
    <tr>
      <td>#<%= ticket.id %></td>
      <td><strong><%= @current_user.new_ticket?(ticket) ? 'new' : '' %></strong></td>      
      <td><%=h ticket.name %></td>
      <td><%=h ticket.category_name %></td>
      <td><%=h ticket.user_username %></td>
      <td><%=h ticket.assignee_username %></td>      
      <td><%= time ticket.updated_at %></td>
      <td><%= link_to "Show", ticket %></td>
      <td><%= link_to "Edit", edit_ticket_path(ticket) %></td>
      <td><%= link_to "Destroy", ticket, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Ticket", new_ticket_path %></p>
