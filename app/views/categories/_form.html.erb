<% semantic_form_for @category do |f| %>
  <% f.inputs do %>
    <%= f.input :name %>
    <%= f.input :parent, :collection => nested_set_options(Category, @category) {|i| "#{'-' * i.level} #{i.name}" } %>
  <% end %>
  <% f.buttons do %>
    <%= f.commit_button 'Save' %>
  <% end %>
<% end %>